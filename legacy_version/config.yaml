server:
  port: "8080"
  readTimeout: "15s"
  writeTimeout: "15s"
  idleTimeout: "60s"

database:
  url: postgres://user:password@db.docker.localhost:5432/go_rest_api?sslmode=disable

jwt:
  secret: your-secret-key-change-in-production
  expirationTtl: "24h"
